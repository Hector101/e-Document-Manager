engines:
  eslint:
    enabled: true
    channel: "eslint-3"
    config:
      config: .eslintrc
    checks:
      indent:
        enabled: false
      import/no-unresolved:
        enabled: false
  duplication:
    enabled: false
    config:
      languages:
      - javascript
ratings:
  paths:
  - "server/**/*.js"
exclude_paths:
  - "server/test/**/*"